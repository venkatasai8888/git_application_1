_schema-version: "3.2"
ID: gitapplication
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: gitapplication-destination-content
  type: com.sap.application.content
  requires:
  - name: gitapplication-destination-service
    parameters:
      content-target: true
  - name: gitapplication-repo-host
    parameters:
      service-key:
        name: gitapplication-repo-host-key
  - name: gitapplication-uaa
    parameters:
      service-key:
        name: gitapplication-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: gitapplication_html_repo_host
          ServiceInstanceName: gitapplication-html5-service
          ServiceKeyName: gitapplication-repo-host-key
          sap.cloud.service: gitapplication
        - Authentication: OAuth2UserTokenExchange
          Name: gitapplication_uaa
          ServiceInstanceName: gitapplication-xsuaa-service
          ServiceKeyName: gitapplication-uaa-key
          sap.cloud.service: gitapplication
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: gitapplication-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: gitapplication-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - gitapplication.zip
      name: gitapplication
      target-path: resources/
- name: gitapplication
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: gitapplication-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: gitapplication-destination-service
    service-plan: lite
- name: gitapplication-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: gitapplication-xsuaa-service
    service-plan: application
- name: gitapplication-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: gitapplication-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
